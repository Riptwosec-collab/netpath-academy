# ─── Phase 1: App Public Variables ───────────────────────────────────────────
# Safe to expose to browser (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_APP_NAME="NetPath Academy"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ─── Future Phases ────────────────────────────────────────────────────────────
# Phase 11+ (Database + Auth):
# DATABASE_URL="postgresql://username:password@host:5432/netpath_academy?sslmode=require"
# NEXTAUTH_SECRET="your-strong-secret-min-32-chars"   # openssl rand -base64 32
# NEXTAUTH_URL="http://localhost:3000"

# Phase 12+ (AI Tutor):
# OPENAI_API_KEY="sk-your-openai-api-key-here"        # server-side ONLY — no NEXT_PUBLIC_
# AI_MODEL="gpt-4.1-mini"
